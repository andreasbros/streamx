// Vite-processed assets - import triggers content hashing
// These resolve to /assets/{hash}/filename-{contenthash}.ext at build time
import logoSvg from "./icons/logo.svg";
import pageBg from "./page-bg.jpg";
import defaultVideoPoster from "./default-video-poster.jpg";

export const LOGO_URL = logoSvg;
export const PAGE_BG_URL = pageBg;
export const DEFAULT_VIDEO_POSTER_URL = defaultVideoPoster;
